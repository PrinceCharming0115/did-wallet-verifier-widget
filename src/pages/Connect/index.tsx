import React from 'react';
import { ConnectContainer } from '../../containers';
import { withLayout } from '../../components/common/Layout';

export const ConnectPage = withLayout(() => {
  return <ConnectContainer />
});