import React from 'react';
import { DetailFlowContainer } from '../../containers';
import { withLayout } from '../../components/common/Layout';

export const DetailFlowPage = withLayout(() => {
  return <DetailFlowContainer />
});